import {MODULE_ID} from "../_moduleId.mjs";

export const defaultIcons = {
    actors: {
        [`${MODULE_ID}.caravan`]: `modules/${MODULE_ID}/icons/cartwheel.svg`
    },
    items: {
        [`${MODULE_ID}.traveler`]: "icons/svg/cowled.svg",
        [`${MODULE_ID}.feat`]: "icons/svg/book.svg",
        [`${MODULE_ID}.wagon`]: `modules/${MODULE_ID}/icons/cartwheel.svg`,
        [`${MODULE_ID}.cargo`]: "icons/svg/barrel.svg",
    }
}